"""
En un archivo llamado playback.py, implemente un programa en Python que solicite al usuario una entrada y luego emita esa misma entrada, reemplazando cada espacio con... (es decir, tres puntos).
"""
user_text = input('')
print(user_text.replace(" ", "..."))